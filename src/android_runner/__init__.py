"""Minimal, movable Android vision-model runner."""

# The one place the version is written down. `pyproject.toml` declares the
# version dynamic and reads this attribute, so the two cannot drift.
__version__ = "0.1.0"
